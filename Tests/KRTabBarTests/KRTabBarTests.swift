import XCTest
@testable import KRTabBar

final class KRTabBarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(KRTabBar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
